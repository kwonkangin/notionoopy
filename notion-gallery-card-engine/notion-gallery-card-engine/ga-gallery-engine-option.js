
<script>
window.GA_CONFIG = {
  // [1] 노션 상단 분류 탭 센서 및 추적 키워드 등록
  tab: {
    selectors: ['span.css-ymcnjv', '.css-1jvn19f', '.css-14pj9fz'],
    keywords: ['전체 보기', '디자인', '기장', '실루엣', '넥라인', '비침정도 (원단두께)', '화이트 톤', '텍스쳐', '상품별']
  },
  
  // [2] 탭 전환 시 카드 메인 뱃지 강제 매핑 규칙 
  // * tagMode 옵션: 'index' (지정순서 추출) / 'auto' (자동 채집) / 'none' (숨김)
  // * tagIndex 지정: 0(1번째 속성), 1(2번째 속성), 2(3번째 속성)...
  rules: [
    { match: 'tab:전체 보기', tagMode: 'index', tagIndex: 2 }
    
    /* 🛠️ 탭별 매핑 확장 시 아래 주석 양식 활용
    ,{ match: 'tab:디자인', tagMode: 'index', tagIndex: 3 },
    { match: 'tab:기장', tagMode: 'index', tagIndex: 4 }
    */
  ]
};
</script>

<script src="https://내-깃허브-주소/notion-gallery-card-engine.js"></script>

<script>
/**
 * =========================================================================
 * 🛒 [Ga-Gallery Engine v2.1] 전역 마스터 제어판 (GA_CONFIG)
 * =========================================================================
 * 본 코드는 노션 데이터베이스의 상단 탭(Tab) 전환을 실시간 감지하여, 
 * 각 탭의 성격에 맞게 갤러리 카드의 속성(뱃지 등)을 동적으로 매핑하는 매니저입니다.
 * * 💡 [배치 순서 규칙]
 * 반드시 깃허브의 'ga-gallery-engine.js' 원본 코드를 호출하기 "직전"에 선언되어야 
 * 엔진이 이 제어판의 명령을 인식하고 구동됩니다.
 * =========================================================================
 */
window.GA_CONFIG = {
  
  // -----------------------------------------------------------------------
  // [1] TAB SENSOR SETTING (노션 상단 분류 탭 감지 센서)
  // -----------------------------------------------------------------------
  tab: {
    /* * selectors: 노션 오리지널 데이터베이스 탭 요소들의 고유 클래스명 리스트입니다.
     * (버전 업데이트로 노션 클래스명이 바뀌면 이 배열에 새 클래스를 추가하면 됩니다)
     */
    selectors: ['span.css-ymcnjv', '.css-1jvn19f', '.css-14pj9fz'],
    
    /* * keywords: 센서를 가동하여 감시할 탭 이름들을 선언합니다.
     * 여기에 등록된 탭을 사용자가 클릭할 때마다 하단의 rules 매핑이 발동합니다.
     */
    keywords: [
      '전체 보기', 
      '디자인', 
      '기장', 
      '실루엣', 
      '넥라인', 
      '비침정도 (원단두께)', 
      '화이트 톤', 
      '텍스쳐', 
      '상품별'
    ]
  },
  
  // -----------------------------------------------------------------------
  // [2] DYNAMIC RENDERING RULES (탭별 갤러리 카드 매핑 규칙 정의)
  // -----------------------------------------------------------------------
  /*
   * 💡 [정밀 가이드 : tagIndex 매커니즘]
   * 노션 데이터베이스 페이지 내부를 열었을 때, 생성해 둔 속성(텍스트, 선택 등)들의 
   * 위에서부터의 순서를 의미합니다. 
   * 단, 컴퓨터 연산은 1이 아닌 '0'부터 시작하므로 아래와 같이 매칭됩니다.
   * * - 첫 번째 속성 = tagIndex: 0
   * - 두 번째 속성 = tagIndex: 1
   * - 세 번째 속성 = tagIndex: 2 (★ 현재 전체보기에 세팅된 값)
   * * 💡 [tagMode 옵션 종류]
   * - 'index' : 지정한 tagIndex 번호의 속성 데이터를 강제로 끄집어내어 카드 메인 뱃지로 노출
   * - 'auto'  : 엔진이 스스로 노션 고유 태그 색상이 들어간 첫 번째 속성을 찾아 자동으로 노출
   * - 'none'  : 카드 상단에 메인 태그 뱃지를 표시하지 않음
   */
  rules: [
    /* * [설명] 사용자가 '전체 보기' 탭을 누르면 -> 데이터베이스 내부의 3번째(index: 2) 
     * 속성값을 긁어와 썸네일 우측 상단 메인 뱃지(.tagBadge_i5j) 자리에 강제로 꽂아넣습니다.
     */
    { match: 'tab:전체 보기', tagMode: 'index', tagIndex: 2 }
    
    /* * 🛠️ [확장 아카이브 : 탭별로 노출할 뱃지를 다르게 설정하고 싶을 때 복사용 레고블록]
     * 아래 주석을 풀고 각 탭이 켜졌을 때 몇 번째 속성(Index)을 메인으로 띄울지 다르게 설계할 수 있습니다.
     * (예: 디자인 탭을 누르면 4번째 속성을 뱃지로 띄우고, 기장 탭을 누르면 5번째 속성을 뱃지로 띄움)
     * * ,{ match: 'tab:디자인', tagMode: 'index', tagIndex: 3 }
     * ,{ match: 'tab:기장', tagMode: 'index', tagIndex: 4 }
     * ,{ match: 'tab:실루엣', tagMode: 'index', tagIndex: 2 }
     * ,{ match: 'tab:넥라인', tagMode: 'index', tagIndex: 5 }
     * ,{ match: 'tab:상품별', tagMode: 'none' } 
     */
  ]
};
</script>

<script src="https://내-깃허브-주소/notion-gallery-card-engine.js"></script>
